experiment_dir: './logs'
interpolate: true
interpolation_size: [128, 128, 128] 
make_logs: true
silent: false

opt:
  device: 1
  criterion: "Dice" 
 
  start_epoch: 0
  n_epochs: 9999

  augmentation: true

  train_batch_size: 1
  val_batch_size: 1

  lr: 0.001 # default - 0.0001 
  save_model: true

model:
  name: "v2v" 
  output_channels: 1 # FCD and background
  max_channel_encoder_decoder: 128 # 
  sigmoid: true
  normalization: 'group_norm'
  # weights: 'logs/v2v_whole_BRATS-all_s128_bs1_GN_DICE_AUG@11.12.2021-02:13:57'
  # weights: 'logs/v2v_whole_BRATS-all_s128_bs1_GN_DICE@10.12.2021-17:26:10/'

dataset:
  features: 'ALL'
  trim_background: true  
  # root: '../fcd_data/normalized_tensors' 
  # root: '../MICCAI_BraTS2020_TrainingData'
  # root: '../fcd_data/patches_dataset_64_features'
  # root: '../fcd_data/data_only_usefull_areas/full/' 
  root: '/nfs/garfield/ibulygin/tensors_v2'
  metadata_path: '/nfs/garfield/ibulygin/metadata_v2.npy'
  # dataset_type: 'brats_interpolated'
  dataset_type: 'interpolated'
  # indexes_selected_root: '../fcd_data/indexes_selected_P0.9_L1250'
  # dataset_type: 'patches'
  # patch_size: 64
